int squareInt(int *arr, int length) {
  for (int i = 0; i <  length; i++) {
    arr[i] = arr[i] * arr[i];
  }
  return 0;
}

int squareFloat(float *arr, int length) {
  for (int i = 0; i <  length; i++) {
    arr[i] = arr[i] * arr[i];
  }
  return 0;
}
